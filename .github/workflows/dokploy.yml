name: Build and Deploy to Dokploy

on:
  push:
    branches:
      - production

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login ke GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push Frontend
        run: |
          docker build \
            --no-cache \
            --build-arg NEXT_PUBLIC_API_URL="https://api.prinora.store/api" \
            --build-arg NEXT_PUBLIC_REVERB_HOST="ws.prinora.store" \
            --build-arg NEXT_PUBLIC_REVERB_PORT=443 \
            --build-arg NEXT_PUBLIC_REVERB_SCHEME="https" \
            --build-arg NEXT_PUBLIC_REVERB_APP_KEY="rkk1ifs5yaarebrdhhzq" \
            -t ghcr.io/${{ github.repository }}/frontend:latest .
          docker push ghcr.io/${{ github.repository }}/frontend:latest

      - name: Build & Push Backend
        run: |
          docker build \
            --no-cache \
            -t ghcr.io/${{ github.repository }}/backend:latest ./backend
          docker push ghcr.io/${{ github.repository }}/backend:latest

      - name: Trigger Dokploy Deploy
        if: github.ref == 'refs/heads/production'
        run: |
          # Webhook URL Dokploy
          curl -X POST "${{ secrets.DOKPLOY_WEBHOOK_URL }}"
